# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  service: one
  timeslot: <%= 2.weeks.from_now %>
  max_slots: 1

two:
  service: two
  timeslot: <%= 2.weeks.from_now %>
  max_slots: 1

three:
  service: one
  timeslot: <%= 3.weeks.from_now %>
  max_slots: 10
